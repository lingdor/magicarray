// Package array is MagicArray main package, Users only import that package can use most features.
// You can use function to create MagicArray Instance:
// array.ValueofStruct
// array.ValueOfSlice
// array.ValueofStructs
// array.MustValueof
// array.ValueofMap
// array.Makeof
//
// array.Makeof can create all supported types to create instance, but need reflect the types to calculate, so use the
// other will be quickly. but array.Makeof is more easily .
package array
